type zygisk_file file_type
typeattribute zygisk_file mlstrustedobject
allow zygote zygisk_file sock_file {read write}

# Post-specialization live loader: connect to the existing daemon through a
# retained directory fd and receive module memfds / getModuleDir descriptors.
allow system_server zygisk_file sock_file {read write}
allow system_server zygote unix_stream_socket connectto
allow system_server {zygote magisk su} fd use
# memfd is sealed against writes but its descriptor is O_RDWR; receiving it
# still requires write permission for the original descriptor access mode.
allow system_server {tmpfs appdomain_tmpfs} file {read write open getattr map execute}
allow system_server adb_data_file dir {read open search getattr}
allow system_server adb_data_file file {read open getattr map}

allow zygote magisk lnk_file read
allow zygote unlabeled file {read open}
allow zygote zygote capability sys_chroot
allow zygote su dir search
allow zygote su {lnk_file file} read

allow zygote adb_data_file dir search
allow zygote zygote process execmem
allow system_server system_server process execmem
allow zygote tmpfs file *
allow zygote appdomain_tmpfs file *
allow zygote proc file *
allow zygote nsfs file *
