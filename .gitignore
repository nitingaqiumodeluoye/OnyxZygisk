.gradle
.idea
.cxx
build
local.properties
Cargo.lock
!zygiskd/Cargo.lock
target
.zig-cache

# WebUI (React + Vite)
zygiskd/webui/node_modules/
zygiskd/webui/dist/

# pnpm's content-addressable store and Playwright's browser bundle live inside
# the workspace so installs do not depend on a writable home directory. Point
# PLAYWRIGHT_BROWSERS_PATH at .playwright-browsers when running the site specs.
.pnpm-store/
.playwright-browsers/

# Agent / tooling scratch
.zcode/
gradle/gradle-daemon-jvm.properties
.claude/
